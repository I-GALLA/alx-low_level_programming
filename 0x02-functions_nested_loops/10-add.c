#include <stdio.h>
#include "main.h"
/**
 * add - Entry point
 * Description: adds two integers and returns the result.
 * @i: and @j: character to be added.
 * Return: the value of the last digit.
 */
int add(int i, int j)
{
	return (i + j);
}
